Config = {}

-- Framework selection: choose between "esx", "qb", or "standalone"
-- This determines which base framework SMX-Core should integrate with
Config.Framework = "esx"

-- General UI settings
-- Default position for menus (e.g. native UI menus)
Config.DefaultMenuAlign = "top-right"

-- Default duration for notifications in milliseconds
Config.DefaultNotifyTime = 5000

-- Default interaction key used in help prompts (e.g. press E to interact)
Config.DefaultHelpKey = "E"

-- Default help text shown to players when they can interact with something
Config.DefaultHelpText = "Interact"

-- Default position on the screen where notifications are displayed
-- Options: "top-right", "top-left", "bottom-right", "bottom-left", 
-- "middle-left", "middle-right", "middle-top", "middle-bottom"
Config.NotifyPosition = "middle-left"
